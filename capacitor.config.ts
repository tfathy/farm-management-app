import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'farm-management-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
